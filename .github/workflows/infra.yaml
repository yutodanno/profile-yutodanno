name: infra
on:
  push:
    branches:
      - main
    paths:
     - 'terraform/**'

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Check for changes in non-excluded paths
        run: |
          echo "Changes between ${{ github.event.before }} and ${{ github.sha }}"
          git diff --name-only ${{ github.event.before }} ${{ github.sha }}
          if git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep -Ev 'terraform/apim/(a|b|c|d|e)/'; then
            echo "Non-excluded paths changed."
            echo "run-job=true" >> $GITHUB_ENV
          else
            echo "Only excluded paths changed."
            echo "run-job=false" >> $GITHUB_ENV
          fi

      - name: Run tasks if non-excluded paths changed
        if: env.run-job == 'true'
        run: |
          echo "Running tasks based on changes in non-excluded paths"
          # ここに実行するスクリプトを追加
