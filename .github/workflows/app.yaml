name: app

on:
  push:
    branches:
      - main
    paths:
      - 'app/**'
  workflow_run:
    workflows: ["infra"]
    branches: [main]
    types:
      - completed

jobs:
  app:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Run echo
      run: echo "Hello, APP"